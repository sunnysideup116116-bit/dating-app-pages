{
  "format_version": 2,
  "sksl": {
    "entrypoint": "interactive_indicator_fragment_main",
    "shader": "// This SkSL shader is autogenerated by spirv-cross.\n\nfloat4 flutter_FragCoord;\n\nuniform vec4 uData0;\nuniform vec4 uData1;\nuniform vec4 uData2;\nuniform vec4 uData3;\nuniform vec4 uData4;\nuniform vec4 uData5;\nuniform vec4 uData6;\nuniform vec4 uData7;\nuniform float uDpr;\nuniform shader uTexture;\nuniform half2 uTexture_size;\n\nvec4 fragColor;\n\nvec2 FLT_flutter_local_FlutterFragCoord()\n{\n    return flutter_FragCoord.xy;\n}\n\nvec4 FLT_flutter_local_textureBilinear(vec2 uv, vec2 physSize, vec2 invPhys)\n{\n    vec2 px = (uv * physSize) - vec2(0.5);\n    vec2 f = fract(px);\n    vec2 p0 = floor(px);\n    vec2 p1 = p0 + vec2(1.0, 0.0);\n    vec2 p2 = p0 + vec2(0.0, 1.0);\n    vec2 p3 = p0 + vec2(1.0);\n    vec2 maxPx = max(vec2(0.0), physSize - vec2(1.0));\n    p0 = clamp(p0, vec2(0.0), maxPx);\n    p1 = clamp(p1, vec2(0.0), maxPx);\n    p2 = clamp(p2, vec2(0.0), maxPx);\n    p3 = clamp(p3, vec2(0.0), maxPx);\n    vec4 c0 = uTexture.eval(uTexture_size * ( (p0 + vec2(0.5)) * invPhys));\n    vec4 c1 = uTexture.eval(uTexture_size * ( (p1 + vec2(0.5)) * invPhys));\n    vec4 c2 = uTexture.eval(uTexture_size * ( (p2 + vec2(0.5)) * invPhys));\n    vec4 c3 = uTexture.eval(uTexture_size * ( (p3 + vec2(0.5)) * invPhys));\n    vec4 cTop = mix(c0, c1, vec4(f.x));\n    vec4 cBot = mix(c2, c3, vec4(f.x));\n    return mix(cTop, cBot, vec4(f.y));\n}\n\nvoid FLT_main()\n{\n    vec2 uSize = uData0.xy;\n    vec2 uOrigin = uData0.zw;\n    vec4 uGlassColor = uData1;\n    float uThickness = uData2.x;\n    vec2 uLightDirection = uData2.yz;\n    float uLightIntensity = uData2.w;\n    float uAmbientStrength = uData3.x;\n    float uSaturation = uData3.y;\n    float uRefractiveIndex = uData3.z;\n    float uChromaticAberration = uData3.w;\n    float uCornerRadius = uData4.x;\n    vec2 uScale = uData4.yz;\n    float uGlowIntensity = uData4.w;\n    float uDensityFactor = uData5.x;\n    float uInteractionIntensity = uData5.y;\n    vec2 uBackgroundOrigin = uData5.zw;\n    vec2 uBackgroundSize = uData6.xy;\n    float uHasBackground = uData6.z;\n    float uAmbientRim = uData6.w;\n    float uBaseAlphaMultiplier = uData7.x;\n    float uEdgeAlphaMultiplier = uData7.y;\n    float uRimThickness = uData7.z;\n    float uRimSmoothing = uData7.w;\n    vec2 fragPx = FLT_flutter_local_FlutterFragCoord();\n    vec2 localLogical = (fragPx - uOrigin) / uScale;\n    vec2 center = uSize * 0.5;\n    vec2 normalizedP = (localLogical - center) / center;\n    float radialDist = length(normalizedP);\n    vec2 halfSize = uSize * 0.5;\n    vec2 q = (abs(localLogical - halfSize) - halfSize) + vec2(uCornerRadius);\n    float dist = (length(max(q, vec2(0.0))) + min(max(q.x, q.y), 0.0)) - uCornerRadius;\n    float smoothing = 1.0 / uScale.x;\n    float mask = 1.0 - smoothstep(-smoothing, smoothing, dist);\n    if (mask <= 0.0)\n    {\n        fragColor = vec4(0.0);\n        return;\n    }\n    vec2 innerHalfSize = halfSize - vec2(uCornerRadius);\n    vec2 p = localLogical - halfSize;\n    vec2 closestOnSkeleton = clamp(p, -innerHalfSize, innerHalfSize);\n    vec2 toEdge = p - closestOnSkeleton;\n    float edgeLen = length(toEdge);\n    vec2 _308;\n    if (edgeLen > 0.001000000047497451305389404296875)\n    {\n        _308 = toEdge / vec2(edgeLen);\n    }\n    else\n    {\n        _308 = vec2(0.0);\n    }\n    vec2 surfaceNormal = _308;\n    vec2 posInBg = uBackgroundOrigin + localLogical;\n    vec2 uvBase = posInBg / uBackgroundSize;\n    float distFromEdge = abs(dist);\n    float edgeZone = 14.0;\n    float edgeInfluence = smoothstep(edgeZone, 0.0, distFromEdge);\n    edgeInfluence *= edgeInfluence;\n    float bendStrength = 0.89999997615814208984375 * (0.5 + (uInteractionIntensity * 0.699999988079071044921875));\n    vec2 edgeOffsetLogical = (((surfaceNormal * edgeInfluence) * bendStrength) * uSize.y) * 0.3499999940395355224609375;\n    vec2 edgeOffsetUV = edgeOffsetLogical / uBackgroundSize;\n    vec2 localRefracted = posInBg - edgeOffsetLogical;\n    vec2 distort = (surfaceNormal * edgeInfluence) * uChromaticAberration;\n    vec2 chromaticShift = distort * 0.119999997317790985107421875;\n    vec3 bg;\n    if (uHasBackground > 0.5)\n    {\n        vec2 physBgSize = uBackgroundSize * uDpr;\n        vec2 invPhysBgSize = vec2(1.0) / physBgSize;\n        if (uChromaticAberration < 0.001000000047497451305389404296875)\n        {\n            vec2 param = localRefracted / uBackgroundSize;\n            vec2 param_1 = physBgSize;\n            vec2 param_2 = invPhysBgSize;\n            bg = FLT_flutter_local_textureBilinear(param, param_1, param_2).xyz;\n        }\n        else\n        {\n            vec2 param_3 = (localRefracted + chromaticShift) / uBackgroundSize;\n            vec2 param_4 = physBgSize;\n            vec2 param_5 = invPhysBgSize;\n            vec3 colR = FLT_flutter_local_textureBilinear(param_3, param_4, param_5).xyz;\n            vec2 param_6 = localRefracted / uBackgroundSize;\n            vec2 param_7 = physBgSize;\n            vec2 param_8 = invPhysBgSize;\n            vec3 colG = FLT_flutter_local_textureBilinear(param_6, param_7, param_8).xyz;\n            vec2 param_9 = (localRefracted - chromaticShift) / uBackgroundSize;\n            vec2 param_10 = physBgSize;\n            vec2 param_11 = invPhysBgSize;\n            vec3 colB = FLT_flutter_local_textureBilinear(param_9, param_10, param_11).xyz;\n            bg = vec3(colR.x, colG.y, colB.z);\n        }\n    }\n    else\n    {\n        bg = uGlassColor.xyz;\n    }\n    float edgeLightCatch = dot(surfaceNormal, uLightDirection);\n    float lc = max(edgeLightCatch, 0.0);\n    float lc2 = lc * lc;\n    float lc4 = lc2 * lc2;\n    float keyHighlight = ((lc4 * lc4) * uLightIntensity) * 0.5;\n    float kc = max(-edgeLightCatch, 0.0);\n    float kc2 = kc * kc;\n    float kc4 = kc2 * kc2;\n    float kc8 = kc4 * kc4;\n    float kickHighlight = ((kc8 * kc4) * uLightIntensity) * 0.5;\n    float rimBrightness = (uAmbientRim + keyHighlight) + kickHighlight;\n    float fresnel = (radialDist * radialDist) * 0.25;\n    float borderMask = 1.0 - smoothstep(0.0, smoothing * uRimSmoothing, distFromEdge - uRimThickness);\n    float bevelGradient = (-surfaceNormal.y) * 0.180000007152557373046875;\n    float modifiedRimBrightness = max(0.0, rimBrightness + (bevelGradient * borderMask));\n    vec3 rimColor = (vec3(1.0) * modifiedRimBrightness) * (uAmbientRim * 10.0);\n    float bgBoost = uSaturation;\n    vec3 _543;\n    if (uHasBackground > 0.5)\n    {\n        _543 = bg * bgBoost;\n    }\n    else\n    {\n        _543 = bg;\n    }\n    vec3 finalColor = _543;\n    finalColor += ((rimColor * borderMask) * 1.5);\n    finalColor += ((vec3(1.0) * fresnel) * uGlowIntensity);\n    finalColor += vec3(uAmbientStrength);\n    finalColor = mix(finalColor, finalColor + (uGlassColor.xyz * 0.20000000298023223876953125), vec3(uGlassColor.w));\n    finalColor = min(finalColor, vec3(1.2000000476837158203125));\n    float standardBaseAlpha = uBaseAlphaMultiplier * mix(0.60000002384185791015625, 1.0, uInteractionIntensity);\n    float baseAlpha = (uHasBackground > 0.5) ? 0.699999988079071044921875 : standardBaseAlpha;\n    float standardEdgeAlpha = uEdgeAlphaMultiplier * mix(0.60000002384185791015625, 1.0, uInteractionIntensity);\n    float edgeAlpha = (uHasBackground > 0.5) ? 0.949999988079071044921875 : standardEdgeAlpha;\n    float glassAlpha = mix(baseAlpha, edgeAlpha, edgeInfluence);\n    float ringOpacity = (borderMask * 0.89999997615814208984375) * clamp(uAmbientRim * 10.0, 0.0, 1.0);\n    glassAlpha = max(glassAlpha, ringOpacity);\n    float alpha = glassAlpha * mask;\n    fragColor = vec4(finalColor * alpha, alpha);\n}\n\nhalf4 main(float2 iFragCoord)\n{\n      flutter_FragCoord = float4(iFragCoord, 0, 0);\n      FLT_main();\n      return fragColor;\n}\n",
    "stage": 1,
    "uniforms": [
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 0,
        "name": "uData0",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 1,
        "name": "uData1",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 2,
        "name": "uData2",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 3,
        "name": "uData3",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 4,
        "name": "uData4",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 5,
        "name": "uData5",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 6,
        "name": "uData6",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 7,
        "name": "uData7",
        "rows": 4,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 0,
        "columns": 1,
        "location": 8,
        "name": "uTexture",
        "rows": 1,
        "type": 12
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 9,
        "name": "uDpr",
        "rows": 1,
        "type": 10
      }
    ]
  }
}